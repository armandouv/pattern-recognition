# pattern-recognition

Pattern Recognition course lab assignments.


## Ejecución

Crear venv:

`python -m venv venv`

`source venv/bin/activate`

`pip install -r requirements.txt`

`python3 p[x]/p[x].py ...`
